# Local Reference Assets

This folder stores local sign reference files for `A-Z` and `0-9`.

The app creates SVG reference files automatically on first launch using:

- `core/content.py` -> `ensure_local_reference_assets(...)`

This keeps the desktop app fully offline-friendly for sign reference cards.
